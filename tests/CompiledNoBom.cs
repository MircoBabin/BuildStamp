namespace BuildStamp
{
    public static class CompilationTime
    {
        // <BUILDSTAMP:BEGINSTAMP>
        public const string COMPILEDATE = "<BUILDSTAMP:COMPILEDATE>";
        public const string COMPILETIME = "<BUILDSTAMP:COMPILETIME>";
        public const string COMPILEDATETIME = "<BUILDSTAMP:COMPILEDATETIME>";

        public const string COMPILEDATEUTC = "<BUILDSTAMP:COMPILEDATE-UTC>";
        public const string COMPILETIMEUTC = "<BUILDSTAMP:COMPILETIME-UTC>";
        public const string COMPILEDATETIMEUTC = "<BUILDSTAMP:COMPILEDATETIME-UTC>";
        // <BUILDSTAMP:ENDSTAMP> 
    }
}
